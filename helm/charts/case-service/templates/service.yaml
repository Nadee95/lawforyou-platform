apiVersion: v1
kind: Service
metadata:
  name: {{ include "case-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "case-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8082
      protocol: TCP
  selector:
    {{- include "case-service.selectorLabels" . | nindent 4 }}
    slot: {{ .Values.activeSlot }}

